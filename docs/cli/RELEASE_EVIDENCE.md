# Release Evidence Command

Run pnpm --filter @gosp/cli start release-evidence foundation to gather existing foundation evidence into machine-readable JSON.

The command reports git SHA, validation mode, simulation default counts, estimate quality report data, and audit status. It gathers evidence only; it does not approve a release or create professional, manufacturing, procurement, or potable-water claims.
