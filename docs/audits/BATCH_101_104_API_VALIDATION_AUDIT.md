# Batch 101-104 API Validation Audit

GO with limitations. /validate defaults to schema-only mode, returns an explicit validationMode, and warns that repository refs are not resolved in the foundation build. Local/dev repo-ref mode is explicit and path-bounded. Production API non-claims are preserved.
