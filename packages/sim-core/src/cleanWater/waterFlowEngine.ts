import type { WaterFlowInput, WaterFlowOutput } from './waterFlowTypes.js'; export function simulateWaterFlow(input:WaterFlowInput):WaterFlowOutput{const warnings:WaterFlowOutput['warnings']=[]; const pumpFlow=input.pumpFlowLpm??1; const eff=input.filterEfficiency??0.8; if(input.pumpFlowLpm===undefined)warnings.push({code:'missing-pump-flow',message:'Missing pump flow spec; defaulted to 1 L/min.',severity:'warning'}); const moved=Math.min(input.sourceLiters,pumpFlow*eff*input.minutes); return {cleanWaterLiters:Number(moved.toFixed(3)),warnings,confidence:{level:warnings.length?'low':'medium',rationale:'Level 1 classroom flow estimate only.'}};}
