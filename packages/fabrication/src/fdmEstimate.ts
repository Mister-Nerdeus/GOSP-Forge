export function estimateFdm(input:{grams?:number;gramsPerHour?:number;setupMinutes?:number}){const warnings:string[]=[]; const gramsPerHour=input.gramsPerHour??20; if(!input.gramsPerHour)warnings.push('Missing print rate; defaulted to 20 g/hour.'); const machineTimeMinutes=Number((((input.grams??0)/gramsPerHour)*60).toFixed(2)); return {machineTimeMinutes,laborMinutes:input.setupMinutes??15,confidence:{level:warnings.length?'low':'medium',rationale:'Foundation FDM estimate only.'},warnings};}
