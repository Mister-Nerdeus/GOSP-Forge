import { z } from 'zod'; import { IdSchema, VersionSchema } from '../shared/primitives.js'; import { DesignModeSchema } from '../modes/designMode.js'; import { RefSchema } from '../shared/ref.js'; import { DesignDocumentSchema } from './designDocument.js';
export const ProjectManifestV2Schema=z.object({kind:z.literal('ProjectManifestV2'),manifestVersion:z.literal('2'),id:IdSchema,version:VersionSchema,title:z.string().min(1),mode:DesignModeSchema,problemRef:RefSchema.optional(),refs:z.array(RefSchema).default([]),design:DesignDocumentSchema}).superRefine((v,c)=>{if(v.mode==='scoring'&&!v.problemRef)c.addIssue({code:z.ZodIssueCode.custom,message:'Scoring projects require problemRef'});});
