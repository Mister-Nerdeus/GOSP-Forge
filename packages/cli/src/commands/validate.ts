import { ProjectManifestV2Schema } from '@gosp/contracts'; import { readJsonFile } from '../exampleRegistry.js'; import { resolveExampleRefs } from '../refResolver.js'; export function validateCommand(file:string){const parsed=ProjectManifestV2Schema.safeParse(readJsonFile(file)); const refs=resolveExampleRefs(); return {ok:parsed.success&&refs.duplicates.length===0,file,schema:parsed.success?'ProjectManifestV2':'invalid',refs};}
