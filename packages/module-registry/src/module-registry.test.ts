import { describe, expect, it } from 'vitest'; import { checkLicenseCompatibility } from './index.js'; describe('license compatibility',()=>{it('blocks unlicensed imports and propagates share-alike obligations',()=>{expect(checkLicenseCompatibility('Unlicensed').publicImportAllowed).toBe(false); expect(checkLicenseCompatibility('CC-BY-SA-4.0').obligations.some(i=>i.includes('share-alike'))).toBe(true);});});
