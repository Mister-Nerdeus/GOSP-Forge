export type LicenseCompatibility={publicImportAllowed:boolean;obligations:string[]}; export function checkLicenseCompatibility(licenseId:string):LicenseCompatibility{if(licenseId==='Unlicensed')return {publicImportAllowed:false,obligations:['Do not import as a public module.']}; const obligations=['Preserve attribution.']; if(licenseId.includes('SA')||licenseId.includes('CERN')||licenseId.includes('ODbL'))obligations.push('Propagate share-alike or data-boundary obligations.'); return {publicImportAllowed:true,obligations};}
